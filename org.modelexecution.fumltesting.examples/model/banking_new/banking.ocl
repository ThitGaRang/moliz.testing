package structure

context Account
inv BalanceRecords: records->select(oclIsTypeOf(Deposit))->collect(amount)->sum()
	- records->select(oclIsTypeOf(Withdrawal))->collect(amount)->sum() = balance

endpackage